import HomePage from "./pages/HomePage/HomePage";

/**
 * @component App
 * @description Root component. The site is a single page, so it just
 * renders `HomePage`.
 */
const App = () => <HomePage />;

export default App;
